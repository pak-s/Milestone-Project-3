import React from 'react';
import ReactDOM from 'react-dom';
import App from './app'; // Update the import statement to use lowercase 'app'

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
